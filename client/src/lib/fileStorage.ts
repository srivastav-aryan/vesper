// Upload a document (store it in documents/{userId}/filename)
// Get a signed URL for access
// Delete a document

// import { supabase } from "@/config/supabase"; 


export const uploadFile = async (userId: string, file: File) => {
  
};


export const getSignedUrl = async (userId: string, fileName: string) => {
  
};


export const deleteFile = async (userId: string, fileName: string) => {
  
};
